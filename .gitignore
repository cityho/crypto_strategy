# 1. 가상환경 및 도구 설정 제외
venv/
.idea/
__pycache__/
*.py[cod]

# 2. 환경 변수 및 보안 파일 제외
.env

# 3. data 폴더는 유지하되 하위 파일은 모두 제외
data/*
!data/.gitkeep

# 4. src 내부의 model 폴더는 유지하되 하위 파일은 모두 제외
src/model/*
!src/model/.gitkeep